import React from "react"

const PageHead = ({ children }) => {

    return (
        <h1 className="text-[3.6rem] text-[#acacac] text-center mb-24 max-md:text-[2.6rem] max-md:mb-16">{children}</h1>
    )
}

export default PageHead
